"""draftzone_mmm.fit_bayes — see docs/ specs. Refactor from docs/prototype_src/ where applicable.

CONTRACT: this module is part of the modeling pipeline and MUST NOT read the sealed answer
key (see CLAUDE.md). Only the evaluation module is permitted to access it.
"""
raise NotImplementedError("Implement fit_bayes per docs/ specs and prototype_src/ reference.")
